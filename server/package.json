{
  "name": "app-server",
  "version": "1.0.0",
  "scripts": {
    "test:app": "cd app && npm i && npm test && cd ..",
    "test:server": "cd server && npm i && npm test && cd ..",
    "test": "npm run test:app && npm run test:server",
    "start": "nodemon server.js"
  },
  "dependencies": {
    "express": "^4.16.4",
    "morgan": "^1.9.1",
    "nodemon": "^1.18.8",
    "pg": "^7.7.1"
  }
}
